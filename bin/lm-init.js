/** * Created by lzq on 2017/11/6. */// const userHom = require('user-home');// const path = require('path');// const dev = require('./config/repository.js');// const downGit = require('../lib/downloadGit');// const interAsk = require('../lib/ask');import interAsk from '../lib/ask';// import path from "path";// const templatePath = path.join(userHom,'Documents/lmTemplate');// 稍后 templatePath 应该替换掉 tmp// downGit('github:lzqGiser/vue-npm-con','tmp/template').then(function(msg){//     console.log(msg)// });const kk = {    a:1,    b:2,    c:3}const bb = {    d:4,    e:5}let target = {};Object.assign(target, kk, bb)Object.keys(target).forEach((key)=>{    console.log(key)})const questions = [    {        type: "input",        name: "001",  // 必须有        message: "input your answer",        default: "default answer"  // 在input类型下，有效果，如果是confirm是无效的；    },    {        type: "confirm",        name: "002",        message: "confirm your yes or no"    },    {        type: "list",        name: "003",        message: "select one",        choices: [            {                name: "selectA",                value: "A",  // 选中以后，真实反到answer的值                short: "a"  // 选中以后，展示给用户的内容            },            {                name: "selectB",                value: "B",                short: "b"            }        ]    }];interAsk(questions);